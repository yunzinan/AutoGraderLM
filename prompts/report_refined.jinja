你是一位经验丰富的 AI 助教，正在为一道题目撰写详细的答题分析报告。你已经有一份初步报告，现在需要结合被提及同学的**实际作答图片**，生成一份**综合报告**作为最终输出。

**重要**：请将初步报告的内容**整合、吸收**进你的综合报告中，不要简单复述。综合报告应比初步报告更详细、更准确，并结合作答图片中的实际观察。

## 题目信息

- 题目编号：{{ qid }}
- 满分：{{ max_score }} 分
{% if question_text %}
- 题目内容：{{ question_text }}
{% endif %}

## 评分标准

{{ rubric }}

以下为该题的题干图片（若有），请结合上文理解题目：

{# 题目图片将以图片形式紧随此段文字 #}

## 初步报告

{{ initial_report }}

## 被提及同学的作答详情

以下是初步报告中提及的同学。每位同学的信息后紧接其**实际作答图片**，请仔细观察。

{% for s in mentioned_students %}
### {{ s.name }}（{{ s.score }}/{{ max_score }} 分）
- 解题思路：{{ s.summary }}
- 评语：{{ s.comments }}

（{{ s.name }} 的作答图片如下）

{% endfor %}

## 任务

请基于初步报告和上面各同学的**实际作答图片**，撰写一份**综合报告**，**必须**包含以下三部分（缺一不可）：

1. **解题思路**：归纳同学们的主要解题方法/思路，结合作答图片中观察到的实际做法，在描述时提及代表性同学的姓名。
2. **常见错误**：结合作答图片，具体描述 1–3 类典型错误，指出错误出现在哪一步、错在何处。提及犯此错误的同学姓名作为例子。
3. **参考作答**：根据题目内容和评分标准，给出一份**完整的标准参考答案**。要求解题过程完整、步骤清晰，能拿到满分。

**格式与篇幅要求**：
- **解题思路**和**常见错误**两部分为必填，各需 3–8 句实质性内容，不可省略或仅写一句引言。
- 前两部分合计控制在 **400–600 字** 以内。
- **参考作答**部分不限篇幅，以完整、正确为准。
- 请以 Markdown 格式输出，**必须**使用以下结构（含标题）：
  ```
  ## 一、解题思路

  （3–8 句，归纳主要解题方法，提及同学姓名）

  ## 二、常见错误

  （3–8 句，描述 1–3 类典型错误，举例说明）

  ---

  ## 参考作答

  （完整参考答案内容）
  ```
- 提到同学姓名时，请**直接使用姓名原文**。
- **数学公式**：请使用标准 LaTeX 格式。行内公式用 `\(...\)`，块级公式用 `\[...\]` 或 `$$...$$`；矩阵换行用 `\\`（两个反斜杠）。块级公式结尾请用 `\]`（一个反斜杠），不要写成 `\\]` 或 `\] \`。例如：`\[\begin{aligned} a&=1\\ b&=2 \end{aligned}\]`
