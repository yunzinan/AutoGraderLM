## 任务

请结合题目图与作业页面图，识别**每道题目的完整作答区域**，输出能包住整道题作答的 bounding box。

**重要**：每道题的 bbox 必须包住该题的**全部作答内容**，包括：
- 每一行文字都要完整包含，不要裁掉半行或半句；
- 多行、多段落的作答要整体包在一个或连续多个 region 内；
- **上下务必多留边距**，宁可框高一些、与相邻题区域有重叠，也绝不能上下裁短漏掉内容；
- 不同题目之间的 region 可以重叠（宁多勿漏）。

## 输出要求

请严格按照以下 JSON 格式输出，不要添加任何其他文字。**bbox 必须使用下面给出的各页像素坐标系（左上角为原点）。**

```json
{
  "questions": [
    {
      "qid": "Q1",
      "regions": [
        {"page": 1, "bbox": [x1, y1, x2, y2]}
      ]
    }
  ]
}
```

其中：
- page：从 1 开始的页码（与下面「第 N 页」对应）
- bbox：[x1, y1, x2, y2]，**必须使用该页的像素坐标**，原点在左上角。**单栏书写时请让区域横跨整页宽度**：x1 取 0（或留少量左边距），x2 取该页宽度（或宽度减少量右边距），主要用 y1、y2 表示该题作答的上下范围即可

## 注意事项

1. **宁大勿小**：每道题的 bbox 必须包住该题**整道题**的作答（整句、整段、整块），**上下尤其要多留边距**，宁可框得高一些、与下一题区域重叠，也绝不能上下裁短。
2. **宁多勿少**：尽量切分出所有题目的作答区域，不要漏掉任何一道题；各题 region 之间**可以重叠**。
3. 注意分栏与跨页：若一道题跨多页，为该题提供多个 region，每个 region 都要包住该页上的完整内容。
4. 请仅输出 JSON，不要包含其他说明文字。
