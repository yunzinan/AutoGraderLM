# AutoGraderLM 使用指南

本文档介绍 AutoGraderLM 的部署方式、使用前配置、功能说明以及完整的工作逻辑链条。

---

## 一、项目部署

### 1.1 环境要求

- Python 3.12+
- Conda（推荐）或 pip

### 1.2 部署步骤

```bash
# 1. 克隆或进入项目目录
cd AutoGraderLM

# 2. 创建虚拟环境
conda create -n autograder python=3.12
conda activate autograder

# 3. 安装依赖
pip install -r requirements.txt

# 4. 启动服务（指定配置文件）
python main.py -c config_hw2.yaml
```

### 1.3 访问应用

启动成功后，在浏览器中访问：

```
http://localhost:8081
```

端口号由配置文件中的 `web_server.port` 指定，默认为 8081。

---

## 二、使用前配置

### 2.1 环境变量（LLM API）

在项目根目录创建 `.env` 文件，配置大模型 API：

```env
# OpenAI 兼容 API 的 Base URL（若使用 OpenAI 官方，可省略）
OPENAI_BASE_URL=https://api.openai.com/v1

# API Key（必填）
OPENAI_API_KEY=sk-your-api-key-here
```

- **OPENAI_BASE_URL**：若使用第三方代理或自建服务，填写其 base URL（如 `https://your-proxy.com/v1`）
- **OPENAI_API_KEY**：调用大模型所需的 API 密钥

### 2.2 作业配置文件

每次作业使用一个 YAML 配置文件（如 `config_hw1.yaml`、`config_hw2.yaml`），主要字段说明：

| 配置项 | 说明 |
|--------|------|
| `assignment_name` | 作业名称，用于界面展示 |
| `assignment_path` | 作业根目录，如 `./hw2/` |
| `web_server.port` | Web 服务端口 |
| `assignment_configuration.pdf_folder_path` | 学生 PDF 作业存放路径（相对于 assignment_path） |
| `assignment_configuration.excel_in_path` | 学生名单 Excel 路径（含学号、姓名等） |
| `assignment_segmentation` | 切分阶段：模型、prompt、并发数、重试次数 |
| `assignment_grading` | 评分阶段：模型、prompt、并发数、重试次数 |
| `assignment_report` | 报告阶段：模型、prompt、导出 Excel 路径 |
| `llm_log` | 是否启用 LLM 对话日志（调试用） |

### 2.3 目录结构准备

在 `assignment_path` 下需准备：

```
hw2/
├── res/           # 学生 PDF 作业（由 pdf_folder_path 指定）
│   ├── 2023010756_彭芊.pdf   # 推荐：学号_姓名.pdf，便于覆盖更新
│   └── ...                  # 也支持 学号_姓名_随机码.pdf，随机码会被忽略
├── in.xls         # 学生名单（由 excel_in_path 指定）
└── (以下由系统自动创建)
    ├── questions/ # 题目配置
    ├── answers/   # 切分后的作答图片（按 学号_姓名 建目录，同一学生更新 PDF 后重跑会覆盖）
    └── results/   # 评分结果 JSON（学号_姓名.json）+ reports/（答题报告 Markdown）
```

**PDF 命名规范**：`{学号}_{姓名}.pdf` 或 `{学号}_{姓名}_xxxx.pdf`。系统仅用学号、姓名作为该生的唯一标识；末尾的 `_xxxx` 可省略，若存在也会被忽略。**更新某生作业时**：在 `res/` 下覆盖该生的 PDF 后重新运行切分/评分，其 `answers/`、`results/` 会自动覆盖，不会产生两条记录。

**Excel 名单格式**：需包含「学号」「姓名」列，以及「成绩（录入项）」「评语（录入项）」等录入列（可为空，系统会写入）。

---

## 三、功能概览

系统提供 7 个主要功能模块，通过左侧导航切换：

| 序号 | 功能 | 说明 |
|------|------|------|
| 1 | **作业配置** | 配置每道题的题干、分值、评分标准、参考答案（文本/图片） |
| 2 | **作业解析** | 将 PDF 按题目切分为图片，支持人工调整框选区域 |
| 3 | **作业评分** | 基于切分结果与题目配置，调用 LLM 逐题评分 |
| 4 | **人工复核** | 对低置信度作答进行人工修正 |
| 5 | **评阅结果** | 按学生查看每道题的得分、评语、评阅人、评阅时间 |
| 6 | **评分统计** | 汇总总分与评语，导出 Excel 录入表 |
| 7 | **答题报告** | 按题目查看得分分布及 LLM 归纳的解题/失分分析 |

### 3.1 作业配置

- 新增/删除题目，每题配置：题目序号、分值、题干文本、评分标准、参考答案文本
- 支持题干图片、参考答案图片：粘贴（Ctrl+V）或选择文件上传
- 配置保存至 `questions/{Q1,Q2,...}/config.json`

### 3.2 作业解析（切分）

- 读取 `res/` 下 PDF，调用 LLM 识别每道题的作答区域（bbox）
- 将 PDF 按 bbox 切分为图片，保存至 `answers/{学号_姓名}/{Q1-0.png, Q2-0.png, ...}`（与 PDF 文件名末尾随机码无关）
- 支持**人工重新切分**：在 for_llm 页面上拖动、新增、删除框选区域，保存后重新生成答案图

### 3.3 作业评分

- 输入：题目配置 + 该题作答图片
- LLM 输出：得分、置信度、总结、评语
- **置信度**：0~4，0/1 不重试；2 会重试直到 ≥3 或达最大次数；3/4 视为可信
- 结果保存至 `results/{学号_姓名}.json`

### 3.4 人工复核

- 对 `confidence ≤ 2` 的作答进行人工复核
- 可修改分数、置信度、评语，提交后更新结果并标记为「人工评阅」

### 3.5 评阅结果

- 按学号选择学生，查看该生每道题的题干、作答图、得分、评语、评阅人、评阅时间
- 支持「AI 重新评阅」单题、「人工复核」跳转

### 3.6 评分统计

- 表格展示：学号、姓名、总分、评语等
- 「生成报告并导出」：生成答题报告 + 导出 Excel（成绩、评语写入配置的 excel_out_path）
- 「下载 Excel」：直接下载当前汇总表

### 3.7 答题报告

- 每题：得分分布柱状图 + LLM 归纳的解题思路与易错点分析

---

## 四、完整工作逻辑链条

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                            AutoGraderLM 工作流程                              │
└─────────────────────────────────────────────────────────────────────────────┘

  ┌──────────────┐     ┌──────────────┐     ┌──────────────┐     ┌──────────────┐
  │ 1. 作业配置   │────▶│ 2. 作业解析   │────▶│ 3. 作业评分   │────▶│ 4. 人工复核   │
  │              │     │ (PDF → 切分)  │     │ (LLM 评分)   │     │ (低置信度)   │
  └──────────────┘     └──────────────┘     └──────────────┘     └──────────────┘
        │                      │                     │                     │
        │                      │                     │                     │
        ▼                      ▼                     ▼                     ▼
  questions/             answers/              results/              results/
  题目配置                切分图片              评分 JSON              人工更新
        │                      │                     │                     │
        └──────────────────────┴─────────────────────┴─────────────────────┘
                                        │
                                        ▼
  ┌──────────────┐     ┌──────────────┐
  │ 5. 评阅结果   │     │ 6. 评分统计   │
  │ (按学生查看)  │     │ (汇总+导出)   │
  └──────────────┘     └──────────────┘
                                │
                                ▼
                        ┌──────────────┐
                        │ 7. 答题报告   │
                        │ (按题目分析)  │
                        └──────────────┘
```

### 4.1 详细流程说明

1. **作业配置**  
   在「作业配置」页面配置每道题的题干、分值、评分标准、参考答案，保存到 `questions/`。

2. **准备材料**  
   - 将学生 PDF 作业放入 `res/`，命名格式：`{学号}_{姓名}.pdf` 或 `{学号}_{姓名}_xxxx.pdf`
   - 准备学生名单 Excel（`in.xls`），含学号、姓名等列

3. **作业解析**  
   点击「开始切分」，系统：
   - 读取题目配置与每份 PDF
   - 调用 LLM 识别各题作答区域（bbox）
   - 按 bbox 切分 PDF 为图片，保存到 `answers/{学号_姓名}/`
   - 若切分不准，可在「人工重新切分」中调整框选并保存

4. **作业评分**  
   点击「开始评分」，系统：
   - 对每道题调用 LLM，输入题目配置 + 作答图片
   - 输出得分、置信度、总结、评语
   - `confidence=2` 时自动重试（最多 `max_retry` 次）
   - 结果写入 `results/{学号_姓名}.json`

5. **人工复核（可选）**  
   - 在「人工复核」页面处理 `confidence ≤ 2` 的作答
   - 修改分数、评语后提交，结果标记为「人工评阅」

6. **评阅结果**  
   在「评阅结果」中按学生查看每题得分、评语、评阅人、评阅时间，可进行单题 AI 重评或跳转人工复核。

7. **评分统计与导出**  
   - 「生成报告并导出」：生成答题报告 + 将成绩、评语写入 Excel 并保存到 `excel_out_path`
   - 「下载 Excel」：下载当前汇总表

8. **答题报告**  
   在「答题报告」中查看每题得分分布及 LLM 归纳的解题思路与易错点。

### 4.2 数据流小结

| 阶段 | 输入 | 输出 |
|------|------|------|
| 作业配置 | 题干、分值、评分标准、参考答案 | `questions/{Qid}/config.json` |
| 作业解析 | PDF + 题目配置 | `answers/{学号_姓名}/{Qid}-{idx}.png` |
| 作业评分 | 题目配置 + 作答图片 | `results/{学号_姓名}.json` |
| 人工复核 | 低置信度结果 | 更新 `results/{学号_姓名}.json` |
| 评分统计 | results + in.xls | 汇总表、导出 Excel |
| 答题报告 | results + 题目配置 | 得分分布 + 归纳分析 |

---

## 五、常见问题

### Q1：如何切换不同作业？

启动时指定不同的配置文件：

```bash
python main.py -c config_hw1.yaml   # 作业 1
python main.py -c config_hw2.yaml   # 作业 2
```

### Q2：LLM 调用失败怎么办？

- 检查 `.env` 中的 `OPENAI_API_KEY` 和 `OPENAI_BASE_URL`
- 若使用代理，确保 `base_url` 正确，且支持 vision 模型
- 可在配置中开启 `llm_log.enabled`，查看 `logs/llm_dialogue.log` 调试

### Q3：切分结果不准确？

- 在「作业解析」→「人工重新切分」中手动调整框选区域
- 可拖动框移动、拖拽角点缩放、新增框、删除框，保存后重新生成答案图

### Q4：置信度含义？

- **0**：丢失全部作答或完全不能理解
- **1**：丢失部分作答或不能理解部分
- **2**：作答完整，但思路无法判断是否正确（会重试）
- **3**：作答完整，基本能理解并判断
- **4**：作答完整，完全能理解并判断
